h2 Список пользователей
.row
  = link_to 'Создать обсуждение', new_community_topic_path, class: 'btn btn-primary'
  = link_to 'Вернуться к группе', [@community], class: 'btn btn-primary'
/p
/  = render 'shared/quick_search', path: accounts_path

table.table
  tbody#index
    - @collection.each do |topic|
      tr
        td
          .row
            .col-md-4
              = image_tag(topic.photo.thumb.url)
            .col-md-4
              = link_to topic.subject, [@community , topic]
            .col-md-4
              - if topic.messages.any?
                = link_to topic.messages.last.account.account.full_name, [topic.messages.last.account.account]
= render 'shared/pagination'

        /td.td-actions.text-right
        /  - if account.account.present?
        /    = link_to [account.account], class: 'btn btn-info btn-simple btn-xs' do
        /      i.fa.fa-account
        /    = link_to [:edit, account.account], class: 'btn btn-success btn-simple btn-xs-lnk' do
        /      i.fa.fa-edit
        /  - else
        /    button.btn.btn-info.btn-simple.btn-xs data-toggle="tooltip" title="У этого пользователя пока что нет профиля"
        /      i.fa.fa-account
        /    button.btn.btn-success.btn-simple.btn-xs-lnk data-toggle="tooltip" title="У этого пользователя пока что нет профиля"
        /      i.fa.fa-edit
        /    /button.btn.btn-danger.btn-simple.btn-xs rel="tooltip" title="Remove" type="button"
            /  i.fa.fa-times
